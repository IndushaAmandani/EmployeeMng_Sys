
        //How we get data from server a obj and push it to associative array then encode that php to  json
        // $sql = "Select id,name,`age`, (SELECT name from gender where id = Gender_id) as gender FROM employee";
        // $result = mysqli_query($db_conn,$sql);
  
        // $employees = array();
        //             while($employee = mysqli_fetch_assoc($result)){
        //          array_push($employees,$employee);
        //             }

        //             $json = json_encode($employees);
        //             echo($json );



